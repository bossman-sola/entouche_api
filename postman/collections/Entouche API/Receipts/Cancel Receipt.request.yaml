$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Cancel Receipt.resources/examples
order: 8000
method: POST
url: "{{base_url}}/api/v1/receipts/{{receipt_id}}/cancel"
headers:
  Accept: application/json
