$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Receive Receipt.resources/examples
order: 7000
method: POST
url: "{{base_url}}/api/v1/receipts/{{receipt_id}}/receive"
headers:
  Accept: application/json
