$kind: http-request
name: Delete Receipt
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Delete Receipt.resources/examples
order: 5000
method: DELETE
url: "{{base_url}}/api/v1/receipts/{{receipt_id}}"
headers:
  Accept: application/json
