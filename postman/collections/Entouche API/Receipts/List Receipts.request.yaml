$kind: http-request
name: List Receipts
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/List Receipts.resources/examples
order: 1000
method: GET
url: "{{staging_url}}/api/v1/receipts"
headers:
  Accept: application/json
