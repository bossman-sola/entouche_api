$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Approve Receipt.resources/examples
order: 6000
method: POST
url: "{{base_url}}/api/v1/receipts/{{receipt_id}}/approve"
headers:
  Accept: application/json
