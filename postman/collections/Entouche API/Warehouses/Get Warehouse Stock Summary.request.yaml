$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Get Warehouse Stock Summary.resources/examples
order: 8000
method: GET
url: "{{base_url}}/api/v1/warehouses/{{warehouse_id}}/stock-summary"
headers:
  Accept: application/json
