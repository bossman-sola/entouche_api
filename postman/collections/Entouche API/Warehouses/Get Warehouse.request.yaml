$kind: http-request
name: Get Warehouse
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Get Warehouse.resources/examples
order: 3000
method: GET
url: "{{base_url}}/api/v1/warehouses/{{warehouse_id}}"
headers:
  Accept: application/json
