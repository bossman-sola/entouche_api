$kind: http-request
body:
  type: json
  content: |-
    {
        "status": "inactive"
    }
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Toggle Supplier Status.resources/examples
order: 6000
method: POST
url: "{{base_url}}/api/v1/suppliers/{{supplier_id}}/toggle-status"
headers:
  Accept: application/json
