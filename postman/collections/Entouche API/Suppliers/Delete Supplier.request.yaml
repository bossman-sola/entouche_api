$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Delete Supplier.resources/examples
order: 5000
method: DELETE
url: "{{base_url}}/api/v1/suppliers/{{supplier_id}}"
headers:
  Accept: application/json
