$kind: http-request
name: List Transactions
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
order: 1000
method: GET
url: "{{base_url}}/api/v1/transactions"
headers:
  Accept: application/json
