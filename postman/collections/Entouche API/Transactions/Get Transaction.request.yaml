$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Get Transaction.resources/examples
order: 2000
method: GET
url: "{{base_url}}/api/v1/transactions/{{txn_id}}"
headers:
  Accept: application/json
