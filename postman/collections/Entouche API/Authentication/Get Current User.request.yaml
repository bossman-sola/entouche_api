$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Get Current User.resources/examples
order: 3000
method: GET
url: "{{base_url}}/api/v1/auth/me"
headers:
  Accept: application/json
