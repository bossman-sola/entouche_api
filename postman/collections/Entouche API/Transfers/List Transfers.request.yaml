$kind: http-request
name: List Transfers
queryParams:
  page: "1"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/List Transfers.resources/examples
order: 1000
method: GET
url: "{{base_url}}/api/v1/transfers?page=1"
headers:
  Accept: application/json
