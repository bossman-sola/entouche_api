$kind: http-request
name: Get Transfer
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Get Transfer.resources/examples
order: 3000
method: GET
url: "{{staging_url}}/api/v1/transfers/{{transfer_id}}"
headers:
  Accept: application/json
