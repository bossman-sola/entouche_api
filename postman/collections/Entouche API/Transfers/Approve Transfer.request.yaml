$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Approve Transfer.resources/examples
order: 7000
method: POST
url: "{{base_url}}/api/v1/transfers/{{transfer_id}}/approve"
headers:
  Accept: application/json
