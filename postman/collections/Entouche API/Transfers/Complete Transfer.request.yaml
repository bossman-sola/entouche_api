$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Complete Transfer.resources/examples
order: 9000
method: POST
url: "{{base_url}}/api/v1/transfers/{{transfer_id}}/complete"
headers:
  Accept: application/json
