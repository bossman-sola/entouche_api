$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Submit Transfer.resources/examples
order: 6000
method: POST
url: "{{base_url}}/api/v1/transfers/{{transfer_id}}/submit"
headers:
  Accept: application/json
