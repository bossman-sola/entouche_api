$kind: http-request
name: List Items
queryParams:
  page: "1"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/List Items.resources/examples
method: GET
url: "{{base_url}}/api/v1/items?page=1"
headers:
  Accept: application/json
