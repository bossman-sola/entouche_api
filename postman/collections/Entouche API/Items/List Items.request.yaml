$kind: http-request
name: List Items
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/List Items.resources/examples
method: GET
url: "{{base_url}}/api/v1/items"
headers:
  Accept: application/json
