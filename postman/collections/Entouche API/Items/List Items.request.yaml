$kind: http-request
name: List Items
queryParams:
  category_id: "3"
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/List Items.resources/examples
method: GET
url: "{{base_url}}/api/v1/items?category_id=3"
headers:
  Accept: application/json
