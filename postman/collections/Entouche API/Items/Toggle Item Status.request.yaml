$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Toggle Item Status.resources/examples
order: 10000
method: POST
url: "{{base_url}}/api/v1/items/{{item_id}}/toggle-status"
headers:
  Accept: application/json
