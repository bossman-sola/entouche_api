$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Get Item Transactions.resources/examples
order: 8000
method: GET
url: "{{base_url}}/api/v1/items/{{item_id}}/transactions"
headers:
  Accept: application/json
