$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Remove Item Image.resources/examples
order: 7000
method: DELETE
url: "{{base_url}}/api/v1/items/{{item_id}}/image"
headers:
  Accept: application/json
