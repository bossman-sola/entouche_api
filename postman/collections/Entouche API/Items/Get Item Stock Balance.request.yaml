$kind: http-request
name: Get Item Stock Balance
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Get Item Stock Balance.resources/examples
order: 9000
method: GET
url: "{{staging_url}}/api/v1/items/{{item_id}}/stock-balance"
headers:
  Accept: application/json
