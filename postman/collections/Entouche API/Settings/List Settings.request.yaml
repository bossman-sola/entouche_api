$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/List Settings.resources/examples
order: 1000
method: GET
url: "{{base_url}}/api/v1/settings"
headers:
  Accept: application/json
