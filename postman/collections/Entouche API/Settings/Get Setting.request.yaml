$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Get Setting.resources/examples
order: 3000
method: GET
url: "{{base_url}}/api/v1/settings/{{key}}"
headers:
  Accept: application/json
