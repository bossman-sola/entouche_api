$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Low Stock Report.resources/examples
order: 4000
method: GET
url: "{{base_url}}/api/v1/reports/low-stock"
headers:
  Accept: application/json
