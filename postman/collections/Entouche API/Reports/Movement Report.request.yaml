$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Movement Report.resources/examples
order: 3000
method: GET
url: "{{base_url}}/api/v1/reports/movement-report"
headers:
  Accept: application/json
