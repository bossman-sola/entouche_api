$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Dashboard Stats.resources/examples
order: 1000
method: GET
url: "{{base_url}}/api/v1/reports/dashboard-stats"
headers:
  Accept: application/json
