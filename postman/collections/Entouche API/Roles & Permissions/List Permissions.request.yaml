$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/List Permissions.resources/examples
order: 3000
method: GET
url: "{{base_url}}/api/v1/permissions"
headers:
  Accept: application/json
