$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Get Role.resources/examples
order: 2000
method: GET
url: "{{base_url}}/api/v1/roles/{{role_id}}"
headers:
  Accept: application/json
