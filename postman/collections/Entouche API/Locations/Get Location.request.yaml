$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Get Location.resources/examples
order: 3000
method: GET
url: "{{base_url}}/api/v1/locations/{{location_id}}"
headers:
  Accept: application/json
