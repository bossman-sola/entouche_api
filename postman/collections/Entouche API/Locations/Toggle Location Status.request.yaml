$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Toggle Location Status.resources/examples
order: 6000
method: POST
url: "{{base_url}}/api/v1/locations/{{location_id}}/toggle-status"
headers:
  Accept: application/json
