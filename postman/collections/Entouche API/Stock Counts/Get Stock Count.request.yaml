$kind: http-request
name: Get Stock Count
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Get Stock Count.resources/examples
order: 3000
method: GET
url: "{{staging_url}}/api/v1/stock-counts/{{stock_count_id}}"
headers:
  Accept: application/json
