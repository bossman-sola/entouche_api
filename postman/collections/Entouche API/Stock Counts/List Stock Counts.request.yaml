$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/List Stock Counts.resources/examples
order: 1000
method: GET
url: "{{base_url}}/api/v1/stock-counts"
headers:
  Accept: application/json
