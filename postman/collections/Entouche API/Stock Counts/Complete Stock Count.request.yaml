$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Complete Stock Count.resources/examples
order: 7000
method: POST
url: "{{base_url}}/api/v1/stock-counts/{{stock_count_id}}/complete"
headers:
  Accept: application/json
