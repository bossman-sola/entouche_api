$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Start Stock Count.resources/examples
order: 6000
method: POST
url: "{{base_url}}/api/v1/stock-counts/{{stock_count_id}}/start"
headers:
  Accept: application/json
