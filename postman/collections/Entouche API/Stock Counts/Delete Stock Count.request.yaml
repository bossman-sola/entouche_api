$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Delete Stock Count.resources/examples
order: 5000
method: DELETE
url: "{{base_url}}/api/v1/stock-counts/{{stock_count_id}}"
headers:
  Accept: application/json
