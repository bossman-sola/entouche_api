$kind: http-request
auth:
  type: bearer
  credentials:
    token: "{{access_token}}"
examples: .resources/Approve Stock Count.resources/examples
order: 8000
method: POST
url: "{{base_url}}/api/v1/stock-counts/{{stock_count_id}}/approve"
headers:
  Accept: application/json
